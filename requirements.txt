# ===== NoneBot2 framework =====
# NoneBot2 core (asyncio event loop, plugin loading, logging)
nonebot2>=2.4.0

# OneBot V11 protocol adapter (talks to NapCat)
nonebot-adapter-onebot>=2.4.0

# ===== Reverse WebSocket driver =====
# NapCat connects to NoneBot2 via WebSocket, so we need FastAPI driver + uvicorn + websockets
fastapi>=0.110.0
uvicorn>=0.27.0
websockets>=10.0

# ===== DeepSeek API =====
# Official OpenAI Python SDK (async AsyncOpenAI client). DeepSeek is OpenAI-compatible.
openai>=1.40.0

# ===== SQLite (async, group chat history) =====
# Async wrapper around sqlite3; used by services/context_store.py so that
# NoneBot2 asyncio handlers never block the event loop on synchronous sqlite3.
aiosqlite>=0.19.0

# ===== Environment variables =====
# Loads the .env file into environment variables
python-dotenv>=1.0.0

# ===== Scheduled tasks (v0.4) =====
# NoneBot official APScheduler plugin: real cron + timezone for morning_greeting
nonebot-plugin-apscheduler>=0.5.0

# ===== Timezone database (Windows) =====
# Python on Windows has no built-in IANA tz database; zoneinfo needs this package
# for BOT_TIMEZONE (runtime date/time state).
tzdata>=2024.1

# ===== Persona RAG（本地向量检索，v0.1 无向量数据库） =====
# NumPy 本地索引 + sentence-transformers CPU 推理（会拉取 torch 依赖）。
# 首次运行 build 脚本时需要联网下载 embedding 模型（默认 BAAI/bge-small-zh-v1.5，
# 约 100MB；网络受限可设 HF_ENDPOINT=https://hf-mirror.com）。
numpy>=1.26,<3
sentence-transformers>=2.6,<4
